<div class="container">
  <div class="row page-header">
    <h1 class="pull-left">Nits Given</h1>
    <%= erb :"nits/nav_links", locals: { user: user, current_page: "given" } %>
  </div>

  <% if nitpicks.any? %>
    <table class="table table-bordered table-striped">
      <thead>
        <tr>
          <th>Given to</th>
          <th>Submission</th>
          <th>Nitpick</th>
          <th>Submitted</th>
        </tr>
      </thead>
      <tbody>
        <% nitpicks.all.each do |nitpick| %>
          <%= erb :"nits/nit", locals: { nitpick: nitpick, user: nitpick.submission_user } %>
        <% end %>
      </tbody>
    </table>
    <div class="pagination">
      <%= will_paginate nitpicks, renderer: BootstrapPagination::Sinatra %>
    </div>
  <% else %>
    <p>
      <b>You haven't given any nitpicks yet.</b>
    </p>
  <% end %>
</div>
