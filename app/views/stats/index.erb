<div class="container">
  <div class="row">
    <br>
    <aside class="col-md-4">
      <nav class="stats">
        <ul class="nav nav-stacked nav-bordered">
          <% tracks.each do |t|%>
          <li class="<%= 'active' if t.id==track.id%>">
            <a href="/stats/<%= t.id %>">
              <%= t.language %>
              <span class="total-count">
                <%= t.problems.size %>
              </span>
            </a>
          </li>
          <% end %>
        </ul>
      </nav>
      <br>
    </aside>

    <main id="track-activity" class="col-md-8">
      <h2><%= track.language %> Activity - Past 30 Days</h1>

      <ul class="graph-legend">
        <li>
          <i class="fa fa-square iterations"></i>
          Iterations
        </li>
        <li>
          <i class="fa fa-square reviews"></i>
          Reviews
        </li>
      </ul>
      <canvas id="track-activity-chart" data-stats='<%= stats.to_json %>' width="800" height="400"></canvas>

    </main>
  </div>
</div>
